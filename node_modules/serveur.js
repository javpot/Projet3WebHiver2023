const http = require('http');
const router = require('../js/router.js');

console.log("DOM fully loaded and parsed");
http.createServer(
    function(request, response){
        router.routeRequest("."+request.url, response);
    }
    ).listen(5510);