const http = require('http');
const router = require('../js/router.js');

console.log("DOM fully loaded and parsed");
http.createServer(
    async function(request, response){
        await router.routeRequest("./assets/content/index.html", response);
        await router.routeRequest("./css/style.css", response);
    }
    ).listen(5500);